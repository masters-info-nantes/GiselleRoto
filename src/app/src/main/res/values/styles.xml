<resources>

	<!-- Base application theme. -->
	<style name="AppTheme" parent="Theme.AppCompat.Light">
		<item name="android:colorPrimary">@color/custom_primary</item>
		<item name="android:colorPrimaryDark">@color/custom_primary_dark</item>
		<item name="android:windowBackground">@color/material_white</item>
		<item name="android:colorAccent">@color/custom_accent</item>
		<item name="android:textColorPrimary">@color/custom_primary_text</item>
		<item name="android:divider">@color/custom_divider</item>
		
		<item name="android:windowNoTitle">true</item>
		<item name="windowActionBar">false</item> <!-- For 2.x version -->
		<item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
	</style>

	<style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
		<item name="spinBars">true</item>
		<item name="color">@android:color/white</item>
	</style>

	<style name="menu_labels_style">
		<item name="android:background">@drawable/fab_label_background</item>
		<item name="android:textColor">@color/white</item>
	</style>
</resources>
